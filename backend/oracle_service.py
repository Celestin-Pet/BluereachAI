import random

def update_carbon_score(token_id):
    # Simulated AI → blockchain update
    new_score = random.randint(50, 200)
    print(f"Updated token {token_id} → score {new_score}")
    return new_score
